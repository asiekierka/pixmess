BLOCKING:
- add map_replace_tile, using the underCounter (see physics.c) to replace a specific under-positioned tile
- add "data" fields to push, pop, get (in a special packet form) and set on the server
- add packet for map_replace_tile, S->C

IMPORTANT:

NEW FEATURES:

MINOR:
- clean up interface.c
